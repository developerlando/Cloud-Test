<%- include('partials/header') -%>

<h1 class="text-4xl">Hello World</h1>

<% if (locals.isAuthenticated) { %>
    <p>Estas Logueado <a href="/profile" class="underline">Mira tu Perfil de AUTH0</a>.</p>
    <button styles="bg-gray-200">Este segundo link hace una peticion a una interfaz interna<a href="/starwar" class="underline"> ir a la peticion</a>.</button>
<% } else { %>
    <p>You are not logged in. <a href="/login" class="underline">Login</a>.</p>
<% } %>

<%- include('partials/footer') -%>
